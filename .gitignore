.env
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
env/
*.db
*.sqlite3
data/

# Local API response cache (regenerable; contains other players' match data)
.match_cache.json

# Personal account config (copy accounts.example.json)
accounts.json

# Riot-sourced data — matches.csv is rebuilt by riftlab.ingest.dataset;
# game_history.json and ddragon/ are local leftovers from removed tools
matches.csv
game_history.json
/ddragon/

# Old bandit weights — the model is now rebuilt from the match cache
.rl_weights.json

# Test runner cache
.pytest_cache/
